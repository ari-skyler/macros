<div class="row bg-primary rounded-top">
  <div class="col text-light p-2">
    <h1>Exercises</h1>
  </div>
  <div class="col p-2 my-auto text-right">
    <a href="/exercises/new" class="btn btn-light my-auto">Create New Exercise</a>
  </div>
</div>
<div class="row bg-light border border-top-0 border-bottom-0 border-primary py-3">
  <div class="col">
    <p class="my-auto">This is a list of shared exercises that any user can access, please do not edit or delete them, unless you created them as changing the exercises affects other users who are useing them in their workouts. It is better to create a new exercise than edit one that has been in use for some time. </p>
  </div>
</div>
<div class="row bg-light rounded-bottom border border-top-0 border-primary mb-3">
  <% @exercises.each_with_index do |x, index_1| %>
    <div class="col-xs-12 col-md-4 col-lg-3 pb-3 px-2 mt-3">
      <div class="card border-primary bg-light mb-3 h-100">
        <div class="card-header">
          <h3><%= x.name %></h3>
        </div>
        <div class="card-body">
            <p>Time based: <%= x.time_based %></p>
            <p> <%= x.calories_burned %> cal <%= x.time_based ? "per min" : " per rep" %> </p>
        </div>
        <div class="card-footer">
          <%= link_to("Permanently Delete", "/exercises/#{x.id}", method: 'delete', :class => "text-danger") %> <br>
          <%= link_to("Edit Exercise", "/exercises/#{x.id}/edit", :class => "text-primary") %>
        </div>
      </div>
    </div>
  <% end %>
</div>
